# Don't re-run `pnpm install` before every `pnpm run` script.
verify-deps-before-run=false
# Treat un-approved build scripts as a warning, not a fatal error.
# (Trusted builds are listed under onlyBuiltDependencies in pnpm-workspace.yaml.)
strict-dep-builds=false
